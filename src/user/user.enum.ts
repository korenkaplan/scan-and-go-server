export enum CardType {
    MAS='mastercard',
    VIS='visa',
    AMR='american express',
}
export enum Gender {
    M= "male",
    F= "female",
}
export  enum Role {
    USER = "user",
    ADMIN = "admin",
}