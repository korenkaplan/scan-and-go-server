*  Remove the nfc chip from collection only after store exit
* Create the Payment Gateway resource (for the card validations);
* add send coupon tom users email every month
* check why create transaction is committed if failed 
* fix stats functions to be more efficient

